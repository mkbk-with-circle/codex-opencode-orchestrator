# Secrets
.env
.env.local
*.pem
config/workspace.local.yaml
config/active.local.yaml

# Local logs (opencode serve etc.)
logs/**
*.log

# Node
node_modules/
packages/bridge/dist/
packages/bridge/*.tsbuildinfo

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# OpenCode local cache (if any)
.opencode/cache/

# Per-workspace orchestrator artifacts (plans/briefs/runs/needs-user)
**/.orchestrator/
