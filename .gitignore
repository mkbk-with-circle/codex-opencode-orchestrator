# Secrets
.env
.env.local
*.pem
config/workspace.local.yaml
config/active.local.yaml

# Local logs (opencode serve etc.)
logs/**
*.log
runs/

# Node
node_modules/
packages/bridge/dist/
packages/bridge/*.tsbuildinfo

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# OpenCode local cache (if any)
.opencode/cache/

# Per-workspace orchestrator runtime artifacts. Plan contracts are versionable.
**/.orchestrator/*
!**/.orchestrator/plans/
!**/.orchestrator/plans/*.md
