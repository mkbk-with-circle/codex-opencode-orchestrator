---
name: opencode-watch
description: >
  已废弃。请改用 $opencode-poll 启动、`$opencode-poll-cancel` 取消。
  若用户仍说 watch，引导他们用这两个命令。
---

# 已废弃

轮询只保留：

1. `$opencode-poll` → `orch poll start --interval N`  
2. `$opencode-poll-cancel` → `orch poll stop`  

详见 `docs/USAGE.md` §5。
